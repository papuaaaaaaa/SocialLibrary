= form_for @dashboard, :html => { :class => 'form-horizontal' } do |f|
  = f.submit nil, :class => 'btn btn-primary'
  = link_to t('.cancel', :default => t("helpers.links.cancel")), dashboards_path, :class => 'btn btn-default'
